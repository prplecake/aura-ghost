image: alpine/edge
packages:
  - npm
  - zip
sources:
  - https://git.sr.ht/~mjorgensen/aura
tasks:
  - init: |
      sudo npm install -g gscan
  - scan: |
      which gscan
      gscan aura
  # - zip: |
  #     zip -r aura aura --exclude "*/.git*"