image: debian/stable
packages:
  - npm
  - zip
sources:
  - https://git.sr.ht/~mjorgensen/aura
tasks:
  - init: |
      npm install gscan
  - gscan: |
      gscan aura
  - zip: |
      zip -r aura aura --exclude "*/.git*"