image: alpine/edge
packages:
  - npm
  - zip
sources:
  - https://git.sr.ht/~mjorgensen/aura
tasks:
  - scan: |
      cd aura
      npm install
      gscan .
  # - zip: |
  #     zip -r aura aura --exclude "*/.git*"