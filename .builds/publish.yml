image: alpine/edge
packages:
  - npm
  - zip
sources:
  - https://git.sr.ht/~mjorgensen/aura
tasks:
  - setup: |
      npm instal gscan -g
  - gscan: |
      gscan aura
  - zip: |
      zip -r aura aura --exclude "*/.git*"